# {{PROJECT_NAME}} — Claude Code Instructions

## Project

<!-- FILL-IN: these are the only three placeholders in this file. Claude Code
     can ask you for them and fill them in (see the README setup message). -->

- **Name:** {{PROJECT_NAME}}
- **Engine:** {{ENGINE}} <!-- e.g. Godot 4.x, Unity 6, Unreal 5.5 -->
- **Description:** {{PROJECT_DESCRIPTION}} <!-- one sentence: what is this game? -->

<!-- GUIDANCE: Everything below this block works as-is — the paths are safe
     defaults (docs/GDD.md, data/_schemas/system_registry.json, …) that get
     created as the studio workflows run. Adjust a path only if your project
     keeps that doc somewhere else. This file is read cold by every new chat —
     it is the ONE doc a fresh session sees before doing anything. Keep it
     tight and current, and delete the GUIDANCE comments as you customize. -->

## How to drive this studio (mode sensing)

Two operating modes ship in this install. **Choose per-request — the user never picks a mode; you sense it:**

- **LIGHT (default):** quick questions, small fixes, early projects, casual asks. Act directly, keep ceremony near zero, end with the recommended next step.
- **FULL:** escalate when signals warrant — a multi-system feature, release preparation, the user asking for rigor/thoroughness, repeated bugs in the same area, or returning after a long gap. Full mode means: director briefs at session open, the complete pipeline (brainstorm → decide → plan → build → verify), gate checks between phases. The full-discipline reference lives in `.claude/docs/skills-protocol-extended.md` and `.claude/docs/director-gates.md`.

De-escalate as readily as you escalate. When unsure, start light and offer: *"want me to run this with full discipline?"*

**Never assign reading.** The docs in `.claude/docs/` are YOUR reference library, not the user's homework. Never answer with "see docs/X" or "read the README" — read it yourself, give them the part that matters, and offer the recommended next step. The user's entire interface is this conversation.

---

## Reading map (read this first every new chat)

You do not need to read every doc before acting. Match the question to ONE row, open that doc, then stop.

| The question is about… | Open this |
|---|---|
| **Current state** — what's built, what's next, why a test does nothing | `data/_schemas/system_registry.json` — the authoritative built-state source, including live test notes. Open first. |
| **Design** — mechanics, pillars, systems intent | `docs/GDD.md` <!-- default path — adjust if your GDD lives elsewhere --> |
| **One system's design in depth** | `docs/gdd/<system>.md`; the decomposition + status table is `docs/gdd/systems-index.md`. |
| **Visual identity** — palette, shape language, asset standards | `docs/art-bible.md` |
| **Architecture decisions** — locked technical/design choices | `docs/adr/` — one ADR per decision, permanent. |
| **System blueprint + programmer rules** | `docs/architecture/` — written on first use, like `docs/adr/` above: `/create-architecture` writes the blueprint, `/create-control-manifest` the programmer rules sheet, `/architecture-review` the requirement registry. Empty until then. |
| **In-progress feature design** — "where's the spec for X?" | `docs/specs/` — check BEFORE restarting a brainstorm. |
| **The plan for work in flight** | `docs/plans/` — the archive lifecycle is in § Project conventions below. |
| **Data / pipeline reference** — formulas, tables, tuning | `docs/implementation-status.md` (points at the data files; numbers live in the data, not here) |
| **Editable game content** — enemies, items, curves | `data/` — one file per thing, schemas in `data/_schemas/`. |
| **Unresolved designer questions** | `docs/open-flags.md` |
| **What changed recently** | `docs/devlog.md` + `git log --oneline` |
| **Last session / next steps / carry-forward** | `production/session-state/active.md` |
| **Project phase** | `production/stage.txt` (mechanical flow state: `production/flow-ledger.yaml`) |
| **Workstream states** — per-domain progress + open questions | `production/workstreams/<name>.md` |
| **Sprint + story state** | `production/sprint-status.yaml`, `production/sprints/`, `production/epics/` |
| **Bugs and test evidence** | `production/qa/` |
| **How strict are reviews right now?** | `production/review-mode.txt` (full / lean / solo) |
| **Which skill do I invoke?** | `.claude/docs/skills-index.md` — skill catalogue + triggers. |
| **Which agent do I dispatch?** | `.claude/docs/agents-index.md` — Intent → Agent table + roster. |
| **Engine gotchas / coding standards** | `.claude/docs/engine-notes/godot-gotchas.md` <!-- bundled default; swap for your engine's notes --> |
| **Project-local runnable checks** | `tools/` — run them, don't reimplement them. |
| **"Where does X live?"** — the whole layout, who writes each file, what's optional | `.claude/docs/doc-stack.md` — the doc-stack contract. Read it when a path is not in this map. |

<!-- GUIDANCE: Add project-specific rows as your doc stack grows. Keep the map
     pointing at ONE authoritative doc per question — never two that can disagree,
     and never two rows naming the same location. This map is the contract the
     skills align to: if you move a doc, move it here first, then update
     .claude/docs/doc-stack.md so the two never disagree.

     ONE HARD RULE: every path this table names must EXIST in a fresh install.
     A row that names a file a skill has not written yet is a lie the very first
     session reads, and the session then hunts for a file that was never there.
     So: artifacts written on first use get a row pointing at their DIRECTORY,
     naming the skill that fills it (see the docs/adr/, docs/specs/, docs/plans/
     and docs/architecture/ rows). Never at the not-yet-written filename. -->

<!-- Everything above resolves on a virgin install. Nothing here is aspirational. -->

---

## Skills Protocol

Prefer firing a skill over improvising its job. If work looks like something a skill covers (a consistency check, a verification pass, a design review), invoke that skill. The user's phrasing is often the trigger — *"run a consistency check"*, *"brainstorm this"*, *"code review"* name skills, not generic verbs.

### The 8 auto-fire skills

The studio fires these automatically when their trigger matches — no permission ask, just a brief announcement:

| Skill | Fires when… |
|---|---|
| `using-superpowers` | Every chat starts. |
| `brainstorming` | Creative work begins — a new feature, mechanic, UI, or behaviour change. |
| `test-driven-development` | Implementation code is about to be written. |
| `systematic-debugging` | A bug / hang / crash / wrong-output report arrives. Evidence-gathering comes before fix attempts. |
| `verification-before-completion` | A "done / fixed / passing" claim, commit, or PR is coming up. Run the verification commands; cite the output. |
| `godot-engine` | Engine files are being edited (scripts, scenes, resources, shaders) or specced. (bundled; anthropic-skills plugin = optional upgrade) <!-- Non-Godot project: swap this row for the matching engine-pack skill --> |
| `session-close` | The session is ending ("wrap up", "we're done"), or 5+ commits have landed without a sync. |
| `writing-skills` | Any `SKILL.md` file is about to be created or edited. |

---

## Dispatch rule

Before non-trivial scoped work, check the `.claude/docs/agents-index.md` Intent → Agent table — if the work maps to a specialist's domain, propose dispatching that specialist first and let the user confirm. Inline is fine for trivial work (small doc tweak, <~50 LOC) or when the user says "do it yourself."

---

## Session open / close

**Open:** read `production/session-state/active.md` + the session-start hook output, surface where the last session left off, and recommend one concrete next action (`/help` logic). Three files, one recommendation, then work.

**Close:** on any wrap-up signal, fire `session-close` — it syncs state files, logs the session, and sets up the next chat to resume cleanly. Full protocol: the `session-close` skill itself.

---

## Pipeline (when to use which path)

Two legitimate paths. The small path is a SUBSET of the large path — it skips the GDD/architecture/epic ceremony. **Default: brainstorm first, THEN choose the path** based on how big the scope turns out to be. Small change that fits in a sentence? No path — just do it.

### Small path (single-system work, 1–2 weeks)

```
brainstorming → architecture-decision (ADR) → writing-plans → executing-plans
```
(+ TDD + verification + ship). Optimal when the work fits one ADR with a few batched decisions.

### Large path (multi-vertical, multi-week)

```
brainstorming → design-system (GDD) → map-systems (decompose)
  → create-architecture → create-control-manifest
  → create-epics → create-stories → dev-story
  → qa-plan → test-driven-development → gate-check
  → finishing-a-development-branch
```
Director gates apply between phases (see `.claude/docs/director-gates.md`). Needed for large, cross-cutting feature areas.

---

## Project conventions

### Spec / Plan / ADR paths

| Skill | Writes to | Naming | Lifecycle |
|---|---|---|---|
| `brainstorming` | `docs/specs/` | `YYYY-MM-DD-<topic>-design.md` | 🚧 IN PROGRESS → ✅ FINAL → 🗄️ ARCHIVED to `docs/z-old/specs/` |
| `writing-plans` | `docs/plans/` | `YYYY-MM-DD-<topic>-plan.md` | 🚧 ACTIVE → ✅ SHIPPED → 🗄️ ARCHIVED to `docs/z-old/plans/` |
| `architecture-decision` | `docs/adr/` | `NNN-<slug>.md` | Permanent. Use `docs/adr/TEMPLATE.md`. Batch 2–3 decisions per ADR. |

Every spec carries a `## Expected outcomes at ship` section near the top — what the designer can do/edit/test once it ships.

### State tracking

`data/_schemas/system_registry.json` is the single source of truth for "is it built?". Update it whenever a chat adds or changes a system, tool, or data category. Trust the registry over any prose doc if they disagree.

### Numbers live in data, not docs

Tunable values (formulas, stat tables, rarity bands, curves) live in editable data files — never hardcoded in a doc or in code. A doc points at the data file and names the tool/editor that edits it. Changing a number = editing data, not editing prose.

---

## Architecture principles

<!-- GUIDANCE: Replace this starter list with your project's actual principles.
     These four are a sane default for a data-driven game; add engine- and
     genre-specific ones (signals, modularity, render boundaries, AI bridges…). -->

1. **Data-driven** — content lives in editable data files under `data/`, never embedded in scripts.
2. **Decomposed** — each system is one unit with a clear boundary; systems communicate through events/signals, not direct reach-in calls.
3. **Modular** — no system assumes another exists. Check for a dependency and degrade gracefully when it's absent.
4. **Human-editable** — every data type has a schema; adding content = adding a data file, not editing code.

---

*This template was scaffolded from ShiningPlague Game Studio. Fill the three `{{…}}` fields in the `## Project` block (everything else has safe defaults) and delete the GUIDANCE comments as you customize.*
